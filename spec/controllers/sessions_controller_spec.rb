require 'spec_helper'

describe SessionsController do
  before(:each) do
    @sessions_controller = SessionsController.new
  end

  it "should set current_user_id on login"

  it "should unset current_user_id on logout"
end